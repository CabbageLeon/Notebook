### 解决的核心问题
目前已经有的有关`affordance`的工作只能在预定义的标签下工作，不利于机器人快速适应广泛的显示场景或操作环境的变化。这篇工作突破了这个限制，通过利用CLIP作为文本编码器允许机器人使用不受限制数量的自然语言文本作为输入。并且具有较强的泛化性。
### 方法

#### 文本编码器
**冻结的** `CLIP` 编码器用于提取文字特征，生成`m`个文字嵌入

#### 3d点云编码器
使用`point-net++`为`n`个输入点依次生成`n`个嵌入向量，并在训练时更新参数。

看了一眼实验报告`iou 14%`，遂弃看
 